info:
  name: Get payment by order id
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/payments/order/{{orderId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
