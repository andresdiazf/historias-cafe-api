info:
  name: Get payment by id
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/payments/{{paymentId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
