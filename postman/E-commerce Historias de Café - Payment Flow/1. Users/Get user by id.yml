info:
  name: Get user by id
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/users/{{userId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
