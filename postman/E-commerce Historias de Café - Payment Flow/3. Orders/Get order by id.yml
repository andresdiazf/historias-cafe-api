info:
  name: Get order by id
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/orders/{{orderId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
