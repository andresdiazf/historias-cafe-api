info:
  name: List products
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/products"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
