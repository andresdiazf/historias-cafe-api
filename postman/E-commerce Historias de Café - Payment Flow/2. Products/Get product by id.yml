info:
  name: Get product by id
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/products/{{productId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
